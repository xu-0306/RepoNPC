Main real-route localization probe, 2026-08-11

Setup: one active public directory whose profile.json declares locale zh-TW and headline
"zh only". Trigger: invoke the real FastAPI application route once with locale=zh-TW and
once with locale=en.

Reproducible command:
rtk proxy uv --cache-dir D:/RepoNPC/.uv-cache run --offline --no-sync --python C:/Python314/python.exe --no-managed-python python .agent-foreman/phase2-review-repairs/evaluation/probe_public_localization.py

Exit code: 1
Observed JSON:
{"en_headline": "zh only", "en_locale": "zh-TW", "en_status": 200, "same_payload": true, "zh_locale": "zh-TW", "zh_status": 200}

One existing Starlette TestClient/httpx deprecation warning was emitted.

Oracle: failed. docs/TECHNICAL_SPEC.md section 9.1 requires the requested locale in the
response contract. The route currently returns the same unvalidated profile.json object for
both supported locale values. A single-bundle bilingual profile producer/schema is not
defined, so Main did not invent one during review.
