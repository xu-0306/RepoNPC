# RepoNPC 修復執行紀錄 — 2026-09-16

## 基準與狀態

- 基準 commit：`94d881a4d1dda2e1362722a8b10f9cd26584bd69`。
- 工作分支：`fix/reponpc-repair-2026-09-16`；不直接更新 `main`。
- 使用者已要求依先前保存的 `REPONPC_REPAIR_PLAN_2026-09-16.md` 進行修正。本文件是 repository 內的執行追蹤，不取代原始完整計畫。
- 初始狀態：GitHub 分支建立已成功；尚未宣告任何修復或測試通過。執行環境的 `git clone` 因 DNS 解析失敗，完整原始碼與測試入口仍在準備。

## 本輪優先順序

1. **R01 批次恢復**：統一可重試資格與實際 budget 檢查；明確區分同輪重試與使用者建立新一輪分析。保留原始成功結果、固定 commit、模型 revision 及歷史記錄，不直接歸零累計次數。所有寫入需保留 owner、交易、單一活動批次及冪等性邊界。
2. **R02 進度與狀態**：已處理與成功分開計數；`completed_with_errors` 在所有元件一致視為終止；正常關閉 SSE 不稱為異常斷線；終止後固定耗時與清除 ETA。
3. **R03 證據資料契約**：統一後端 `excerpt` 與前端顯示；在 API 邊界驗證資料，而不是僅做 TypeScript 型別斷言；引用仍由伺服器建立。
4. **R04 錯誤資訊**：保留安全的錯誤碼、request ID、階段與實際取得的狀態。網路回應遺失時先核對快照，不直接宣告失敗並重複送出。
5. **R05 貢獻潤飾**：將口語整理成自然、忠於原意的雙語作品集文字；不增加工作或成果、不轉移 AI／協作者貢獻；未確認狀態只存在於 metadata／介面。獨立限制輸出 budget，檢查截斷與 Schema。
6. **R08 預覽相容性**：修復 `data:` 圖片與正式 `img-src 'self'` 的衝突，優先使用受保護、短效、有限容量的同源預覽，不全面放寬 CSP。
7. **R11 聊天上下文**：畫面歷史與送入模型的歷史分離；只傳完整成功問答輪次，遵守伺服器數量、字數與角色交替限制。

## 後續項目（未完成前不能勾選）

- R06：貢獻簡化編輯、雙語同步及重新生成時的草稿保留。
- R07：正常技術主體句的人物歸因誤判；不能單純移除安全驗證。
- R09／R10：NPC 外觀編輯、七狀態動畫預覽、卡片排版／格式與主題選擇。
- R12：角色狀態與真實請求事件對齊，不將 buffered delivery 宣稱為模型逐 token 串流。
- R13：真正可重用的衍生索引快取；先量測，再增加保存與失效機制。
- R14：活動與最近終止批次、未確認草稿的安全恢復。
- R15：從草稿到明確發布、bundle 啟用與 GitHub README 的完整流程。

## 必須保留的界線

不覆寫既有公開 bundle／上一可用版本，不提交 runtime.sqlite、`.env`、金鑰、私有服務位址、原始 provider 回應或備份。使用者確認之前的草稿不得變成 `OWNER_ASSERTION`。不更換 RAG 框架，不引入付費平台，不自行合併 PR 或觸發正式發布。

## 驗收清單

- [ ] 兩成功、一失敗且已用完三次生成的情境可恢复，成功結果不遺失。
- [ ] 重複點擊／回應遺失不建立重複工作；暫停／取消不會被重試復活。
- [ ] 顯示「已處理 3/3；成功 2；失敗 1」，結束後不殘留 ETA 或斷線警告。
- [ ] 真實後端 `excerpt` payload 可顯示證據內容；無效資料不假裝成功。
- [ ] 口語「我想功能，AI 寫程式」不被擴寫成測試／部署／驗收經歷；草稿標籤不寫入正式內容。
- [ ] 截斷及無效貢獻輸出被拒絕，原始說明保留。
- [ ] production CSP 下預覽可見；未登入／過期不可取得未發布素材。
- [ ] 至少十輪聊天與失敗重試仍符合歷史限制。
- [ ] 實際執行的單元、API、前端與瀏覽器測試另記錄結果；未執行的測試不得宣稱通過。
- [ ] 交付包含所有修改檔案的完整程式，而不是只有片段。

## 驗證與交付紀錄

待實作後填入實際命令、結果、commit、未完成項目與回復方式。既有 handoff 的歷史通過數不是本輪測試證據。真實 provider、clean-host Docker 與 GitHub Profile 顯示需分開驗收。
